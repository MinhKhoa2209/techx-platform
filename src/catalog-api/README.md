# Catalog API

Static product catalog HTTP API.

```powershell
npm run dev -w @techx/catalog-api
npm run test -w @techx/catalog-api
```

Endpoints: `GET /healthz`, `GET /readyz`, `GET /api/products`, and `GET /api/products/{id}`.

